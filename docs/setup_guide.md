# 🎯 达人营销运营看板（飞书多维表格版）说明文档

此多维表格是为了支持团队协同、进行达人营销的精细化运营而搭建的。系统利用 `lark-cli` 自动化构建，并预填了业务场景模拟数据。

---

## 🔗 访问链接

> 📊 **推荐：飞书客户端原生仪表盘（免服务器、实时更新、直接在飞书里看）**
>
> **[点击打开飞书原生仪表盘](https://my.feishu.cn/base/E4Aub48pUadmyBskPEjcML63nrh?table=blkXj8bScJcIrmMD)**
>
> *我们在表格内部已经为您创建了官方原生**「项目运营协同大盘」**仪表盘！包含达人总数指标卡、实际已花费指标卡、物料流水线负荷分布环形图。您的同事和您直接打开该链接即可共享查看，且数据完全实时，无需手动或后台同步！*

---

## 🖥️ 外部高级大屏定制查看（GitHub Pages 公网大屏）

如果您需要将看板展示在独立的投屏大屏幕上，或者需要极具极客质感的“暗黑毛玻璃”风格排版，可以使用我们为您打包部署的外部大屏：

> 🔗 **点击下方链接直接在浏览器打开公网大屏：**
>
> **[公网大屏网页 (GitHub Pages)](https://gyzwang.github.io/KOL-Marketing-Kanban/)**

### 如何更新外部大屏数据：
每当您在飞书表格中修改了数据，只需双击运行本地脚本即可同步到云端数据库：
```bash
./refresh.sh
```
*这会触发数据抓取并自动将最新的 JSON 数据安全上传至您的 GitHub Gist 云端存储。*

---

## 💻 飞书内嵌外部高级大屏（可选）

如果您希望在飞书仪表盘中内嵌这个**“外部高级暗黑大屏”**，可以：
1. 打开您的 [飞书多维表格](https://my.feishu.cn/base/E4Aub48pUadmyBskPEjcML63nrh)。
2. 点击表格标签栏右侧的 **“+”** 号 ➔ 选择 **“新建仪表盘”**。
3. 在右侧组件库中，选择 **“网页”** 组件并拖拽入画布中。
4. 在网页 URL 输入框中填入：`https://gyzwang.github.io/KOL-Marketing-Kanban/`，保存。
5. 同事即可在飞书内直接访问这个高级定制大屏。

---

## 📈 学员沟通反馈同步 (2026-07-03)

针对学员（达人）最新沟通动态，已运行同步脚本：
- **同步数据**：扎西云旦、格桑次仁、周家松、洛桑旦巴（抖音平台，执行中）。
- **异常预警拦截**：由于学员 **洛桑旦巴** 爷爷手术原因暂时失联，脚本已自动在 `📋 任务看板` 生成 P0 紧急的已延期任务，并在 `📦 物料管理` 自动录入了“后续素材包”断更预警跟踪，防止内容断更。
- **运行命令**：
  ```bash
  python3 scripts/update_feedback.py
  ```

---

## 📂 多维表格架构设计

应用内包含 **4 张核心数据表**，并分别为其创建了直观的**看板视图**与**甘特图视图**：

### 1. 📋 任务看板 (`tblxj1B7MGMgU7Dj`)
用于团队每日任务的可视化跟进、优先级排列及延期警示。
- **主视图**：`任务进度看板` (按“任务状态”分组的看板)
- **甘特图**：`任务甘特图` (按截止日期排期的甘特图)
- **核心字段**：所属模块、任务状态、优先级、负责人、截止日期、延期天数、关联达人。
- **协同方式**：可以直接在看板中拖拽任务卡片调整状态。超期任务建议调整状态为 `已延期`，并补全延期天数。

### 2. 📦 物料管理 (`tbligeEr1TQzKBau`)
物料生命周期流水线跟进（此版已根据最新反馈**去掉拍摄环节**，以达人提供素材后的团队剪辑与发布为主）。
- **流程链路**：
  `待接收素材` ➔ `素材已到` ➔ `剪辑中` ➔ `封面优化中` ➔ `标题优化中` ➔ `待审核` ➔ `已发布`
- **主视图**：`物料状态流水线` (看板视图)
- **甘特图**：`发布甘特图` (甘特图视图，展示素材接收到预计发布排期)
- **核心字段**：剪辑完成、封面完成、标题简介完成、审核通过（均为复选框，方便确认各环节就绪）。

### 3. 👤 达人管理 (`tblhv1jMCaVmeXXe`)
KOL 合作建联及档案库管理。
- **主视图**：`合作状态看板` (按“合作状态”分组的看板，跟踪建联到合作结束的全生命周期)
- **核心字段**：平台、粉丝量、合作类型、合作费用、对接人、沟通记录、交付物料统计。

### 4. 💰 预算管控 (`tblpYwoTY9dH5lX9`)
前置预算执行风险预警，支持**目标-动作-成本**强绑定映射。
- **主视图**：`预算状态预警` (看板视图，直观展示消耗情况，红黄绿预警)
- **核心字段**：预算金额、实际花费、消耗率、关联目标、目标指标、对应动作、预警状态。
- **规则嵌入**：
  - 消耗率在 80%~95% 之间 ➔ `⚠️黄色预警(≥80%)`
  - 消耗率在 95%~100% 之间 ➔ `🔴红色预警(≥95%)`
  - 消耗率超过 100% ➔ `🚨已超支`

---

## 🛠️ 文件资产目录

```text
/Users/gzm/Desktop/看板搭建/
├── 看板搭建.m4a           # 原始需求音频
├── index.html            # 🖥️ 可视化看板大屏主页面（支持飞书仪表盘网页组件内嵌）
├── refresh.sh            # 🔄 一键手动同步脚本
├── scripts/
│   ├── setup_bitable.py      # lark-cli 自动化建表与数据初始化脚本
│   ├── fetch_data.py        # lark-cli 数据提取与清洗脚本
│   ├── server.py            # 🚀 自动同步 Web 服务器脚本
│   ├── data.js              # 💾 本地数据库缓存
│   └── render.js            # 🧠 页面数据渲染与交互逻辑
├── styles/
│   └── main.css            # 🎨 全局暗黑毛玻璃设计系统样式表
└── docs/
    └── setup_guide.md    # 本使用说明文档 (本文件)
```
